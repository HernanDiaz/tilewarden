<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Placeholder Android XML theme. The real styling is in Kotlin via
        MaterialTheme inside MainActivity#setContent. This base theme just
        ensures the system bars don't paint over the splash before Compose
        takes over.
    -->
    <style name="Theme.Tilewarden" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">#2A1A14</item>
        <item name="android:navigationBarColor">#2A1A14</item>
        <item name="android:windowBackground">#2A1A14</item>
    </style>
</resources>
