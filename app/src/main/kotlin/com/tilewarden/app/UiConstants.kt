package com.tilewarden.app

/**
 * UI-wide tuning constants. Kept in one tiny file so multiple Composables
 * can share them without growing each one's import list.
 */

/** Opacity used for heroes that already took their turn this round. */
internal const val ACTED_ALPHA: Float = 0.45f
